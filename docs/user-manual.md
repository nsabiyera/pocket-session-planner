# User manual

Pocket Session Planner is for the twenty minutes before training and the hour during it. Plan
a session on the bus, run it with cold thumbs in the rain, and answer *how did it go* before
you get back to the car.

Everything lives on your phone. There is no account, no signal needed, and nothing leaves the
device unless you export it.

- [Before your first session](#before-your-first-session)
- [Planning a session](#planning-a-session)
- [Player challenges](#player-challenges)
- [Running a session](#running-a-session)
- [Reviewing it](#reviewing-it)
- [Between sessions](#between-sessions)
- [Players and progress](#players-and-progress)
- [The two FA models](#the-two-fa-models)
- [Your data](#your-data)
- [When something looks wrong](#when-something-looks-wrong)

## Before your first session

On first open the app asks for one thing: **a squad name**. That is the whole setup.

Then go to **Squad** and add your players. Either type them one at a time, or tap
`Paste a whole team list` and paste the names from wherever you already keep them. Lines or
commas both work, and shirt numbers are optional — `7 Kai Roberts`, `7. Kai Roberts` and
`Kai Roberts` all mean the same thing.

`Default session length` on the same screen is the number every new session starts at. Set it
once to whatever your slot actually is.

`Remove` does one of two things, depending on what it would cost you. A player you have never
logged anything against is simply deleted — it was a typo. A player with history is
**archived** instead: they leave the roster, their history is kept, and the toast offers
`Undo`. `Show archived players` brings them back into view.

## Planning a session

**Plan** asks four questions, in the order a coach thinks about them.

**What are you working on?** Fourteen objectives from the curated library, or `Something else`
to type your own. Picking one from the library brings its success criteria and a few coaching
points with it, so the practice arrives half-written.

**How will you coach it?** Five methodologies — Play-Practice-Play, Constraints-Led, Guided
Discovery, Whole-Part-Whole, Command / Direct. **This is what builds the session**: the phases,
their lengths and their coaching intent are all derived from the one you pick. Changing it
re-derives them. The app remembers your last choice, so the control is usually already right.

**Intervention** is one read-only line — how you intend to coach, and how much. On the default
path it costs zero taps. `Edit` opens the detail if you want it: the method, the mechanic, and
budgets for `Max interventions per phase` and `Max seconds per stoppage`, per phase if needed.

**How long** and **Focus players.** Focus players are the two or three you have decided to
watch today. They get a tappable chip in Do mode, and the review asks about them by name.

`Build session` produces the phases. On that screen you can retitle or re-time any phase,
reorder them with `↑` `↓`, set [player challenges](#player-challenges), or `Discard this plan`.
Then `Start session ▸`.

> Squad home to a running session should take under twenty seconds and no typing. If it is
> taking longer, you are working harder than the app intends.

## Player challenges

A challenge is **one thing one player is trying to do today** — *"Kai, three forward passes"*.
It is not a coaching point. A point is something you deliver once and tick; a challenge is
something you count, and then rule on.

From the phase editor, `Player challenges → Set`. For each player:

- **The challenge** — what you have asked of them, in your words.
- **How will you judge it?** `Count it` with a target, or `Judge it` for the things that
  cannot be counted (*stay positive when you lose it*). A judged challenge shows no tally,
  because a number nobody believes is worse than no number.
- **Narrow it down** — optionally limit it to certain phases (*use your left foot in the
  rondo*), and tag which corner it develops.

Any player can hold a challenge, not just your focus players. Giving a quiet player one thing
to do is often exactly how they stop needing to be a focus player.

In Do mode each live challenge is one row. **Tap the row to count it.** The tally is the
number of times you tapped — never a stored counter — so `Undo` on the toast removes the last
sighting rather than decrementing a number that could drift. A counted challenge reads as met
the moment it reaches its target; you do not have to confirm what the tally already answered.

The `⌄` on a row opens the verdict: **Met / Partly / Missed**, plus `＋1` if you have lost
count. `Partly` exists because the honest answer to *did Kai make three forward passes* is
often "he made one, and he was looking for them".

Anything you have not ruled on can be settled in the review.

## Running a session

Do mode is a mode, not a page: no navigation, nothing to mis-tap.

**The timer is a wall clock, not a countdown.** Lock the phone, take a call, let the browser
throw the tab away — when you come back it shows the time that has actually passed, because
the drill genuinely was still running.

- **The phase name at the top** is a button. It opens phase options: `−5 min` / `+5 min`,
  `Jump to` another phase, or `Abandon this session` (everything logged is kept — twenty
  minutes of evidence is still evidence).
- `⏸ Pause` stops the clock. `Next phase ▸` moves on.
- **`✋ Intervene` is one tap**, pre-filled from your plan, because the common case is you
  doing what you said you would. A stop-play mechanic pauses the phase clock automatically, so
  ball-rolling time measures itself. Long-press it (right-click on a desktop) for the detail
  sheet, where you can change the method, aim it at one player, or add a note.
- `＋ Note` for a team-wide note.
- `End` finishes and takes you to the review.

### Logging an observation

**Tap a player chip, then `Good` / `Working` / `Struggled`.** Two taps, and it is saved.
Everything else on the sheet is optional:

- **When in the action** — `Before` / `Receiving` / `After`. Which part of the action you were
  watching. Nothing can infer this; only you know whether you were watching the scan or the
  touch.
- **Core capabilities** — the FA's six, in their own words.
- **This phase** — your own coaching points for the phase you are in.
- **The four corner groups** — the specific attribute you saw.

Tapping a tag is what makes the reports work. The corner and the capability come along for
free from the tag you tapped, rather than being extra questions.

## Reviewing it

Five taps and no required typing.

The objective is restated verbatim — `Yes` / `Partly` / `No` — then which success criteria you
actually got, and a rating out of five.

**What the app noticed** is the part you only confirm. Depending on the session it will tell you:

- which phases ran over, and by how much;
- any focus player you logged nothing against — the omission coaches most want flagged;
- **your challenges**: *"2 of 3 challenges met — 1 partly."*;
- **your intervention record**: how many times you stopped play against how many you planned,
  and your ball-rolling time;
- **your 4 Corner coverage**: *"9 observations for this session: 8 technical, 1 social —
  nothing physical or psych."*;
- **which capabilities you watched**, once eight or so observations carry one;
- **when in the action you were watching**, once you have recorded four moments. The last
  clause is the one worth having: *"6 moments recorded: 5 as they receive, 1 after they
  receive — nothing before the ball arrives."* A coach who only ever watches the touch cannot
  see that about themselves, and no amount of logging reveals it on its own.

Then **Last time you said…** (the points that seeded this session — did they move on?),
**Focus players** (moved on / same / went back, plus a next step), and **Challenges**, where
you tap a verdict to settle anything still open. Tap the same verdict again to take it back.
A judged challenge can only be settled here — Do mode has gone by now.

**Carry into next session** is a checklist the app has worked out: revisit the objective, keep
focusing on a player, a coaching point you did not get to, a nudge about your intervention
habit. Untick anything you do not want. **Nothing is written until you tap `Save review`.**

## Between sessions

**Today** is the home screen. It shows the session in progress or the plan you were part-way
through, everything carried forward from your last review with a `Use these` button that
pre-fills a new session, your last session with `Repeat`, and the recent ones.

`Repeat` clones a session into a fresh draft: same objective, same phases, coaching points
reset to undelivered, challenges repeated as asks with last week's verdicts cleared.

**History** lists every session by date with its methodology and status. Tapping one shows its
phases, everything you logged, and its review.

## Players and progress

**Squad → a player** is their profile.

- **4 Corner balance** — the share of your observations of them by corner, and the corner the
  app would nudge you towards. This is derived from what you already log; it costs nothing.
- **4 Corner check** — four taps, occasionally, to record where they are now. Two checks give
  you a delta (`+1`, `−1`) on each corner. Rate what you can; skipping a corner is better than
  guessing at one.
- **Under the microscope** — one player, one skill, the FA's six core capabilities. Pick the
  skill (intercepting, pressing, passing, tackling, finishing, turning), then rate what you
  saw. Each row shows what your own observations already say about that capability, so you are
  confirming or overruling something rather than trying to remember. It never fills in a
  rating for you. Two scans of the same skill give you deltas, and `Work on next` records the
  one capability you intend to chase.
- **Timeline** — everything you have logged about them, newest first.

## The two FA models

**The 4 Corner Model** — technical/tactical, physical, psychological, social — exists to stop
you developing a quarter of a player. The app makes your bias visible rather than telling you
off for it: log what you see, and the balance report says what you have been looking at.

**The six core capabilities** — Scanning, Timing, Movement, Positioning, Deception,
Techniques — are the framework for putting one player under the microscope on a single action,
across what they do *before, during and after they receive the ball*.

Honest about provenance: the six capability names and their descriptions are the FA's, and so
is the before/during/after window. The attribute lists inside each corner, the mapping from
those attributes onto capabilities, and the corner a capability tag files under are this app's
own choices — grounded in the models, but not official FA taxonomy.

## Your data

Everything is stored on the device, in the browser, and works with no signal.

- **Install it.** Settings explains how for your browser. Installed, it opens like an app with
  no browser chrome, and the home-screen icon gets shortcuts for `Start a session`, `Resume`
  and `Squad`.
- **Export everything** writes a single JSON file — `psp-u12-reds-2026-05-19.json` — that
  contains your squad, players, sessions, observations, reviews, carried points, 4 Corner
  checks and capability scans. **It is the only backup there is.** Do it at the end of a term.
- **Import a file** previews what it will do before touching anything: how many rows are new,
  updated or unchanged, and anything it will drop and why. Importing your own export is a
  clean no-op, and importing another coach's squad merges alongside yours rather than
  replacing it.
- **Storage.** Browsers can clear site data under pressure. If Settings says storage is not
  protected, `Ask to protect it`.

## When something looks wrong

**"This session has been open a while."** You forgot to end a session and it has been running
for hours. The banner offers to end it at the time of your last activity rather than claiming
a five-hour practice. It offers; it never assumes.

**A mis-tap.** Almost everything shows a toast with `Undo` — observations, challenge counts,
verdicts. On a rating, tapping the same score again clears it.

**A tally looks wrong.** Open the challenge's `⌄` sheet: it shows the tally and the count for
this phase, with `＋1` if you missed one. `Undo` on the toast removes the most recent sighting.

**A player is missing.** They may be archived: `Show archived players` on the Squad screen.

**A number you did not expect.** Every report on the review screen is derived from what you
logged, and nothing is inferred that cannot be. If a line says nothing about a corner or a
capability, it is because no observation carried one — not because the app decided for you.

**None of the above.** `Settings → Feedback` writes it up for you. Pick whether something
broke or you want something changed, type what happened, and tap `Open GitHub to post it` —
GitHub opens with the report already written, and it is posted only once you press `Create`
there, under your own GitHub name. Nothing leaves the phone until you do.

Two things worth knowing. `Device details` attaches the app version, your browser and your
screen size, which is usually what turns "the timer froze" into a fix; you can leave it off,
and either way **nothing about your squad, players or sessions is ever attached**. And if you
write the note pitch-side with no signal, it is saved on the device — come back to Settings
when you are back online and the words are still there.
